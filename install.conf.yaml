- defaults:
    link:
      relink: true

- link:
    ~/.ctags: ctags
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zprofile: zprofile
    ~/.zsh: zsh
    ~/.gitconfig: gitconfig
    ~/.git_commit_template: git_commit_template

- brew:
    - chruby
    - ruby-install
    - ctags
    - git
    - vim
    - zsh
    - zsh-completions
    - direnv

# set shiftwidth=2 tabstop=2 expandtab
