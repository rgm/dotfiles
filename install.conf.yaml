- defaults:
    link:
      relink: true

- link:
    ~/.ctags: ctags
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zprofile: zprofile
    ~/.gitconfig: gitconfig
    ~/.git_commit_template: git_commit_template

- brew:
    - git

# set shiftwidth=2 tabstop=2 expandtab
