- defaults:
    link:
      relink: true

- link:
    ~/.ctags: ctags
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.zprofile: zprofile
    ~/.gitconfig: gitconfig
    ~/.git_commit_template: git_commit_template

- brew:
    - chruby
    - ctags
    - git
    - vim
    - zsh
    - zsh-completions

# set shiftwidth=2 tabstop=2 expandtab
